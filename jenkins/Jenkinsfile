pipeline {

    agent any

    tools {nodejs "nodejs"}

    environment {
        CI = 'true'
    }
    
    stages {
        stage('grant permission'){
            steps {
                sh "chmod +x -R ${env.WORKSPACE}"
            }
        }

        stage('Install dependencies') {
            steps {
                sh 'cd api && npm install'
            }
        }
        stage('Test') {
            steps {
                sh './jenkins/scripts/test.sh'
            }
        }

        stage('docker build image') {
            steps {
                sh 'docker build -t gsdatabase/api . -f Dockerfile.prod'
            }
        }
        
        stage('Deliver') {
            steps {
                sh 'echo finish'
            }
        }
        
    }
}